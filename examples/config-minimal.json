{
  "_comment": "Minimal Configuration Example",
  "_description": "Only override essential settings, use defaults for everything else",

  "core_settings": {
    "INSTALLER_OS": "15",
    "MAX_DEFERS": 3
  },

  "feature_toggles": {
    "TEST_MODE": false,
    "DEBUG_MODE": false
  }
}
